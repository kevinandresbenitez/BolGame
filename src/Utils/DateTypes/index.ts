type position ={
    positionX:number,
    positionY:number
}

type direction ={
    directionX:number,
    directionY:number
}

type collisionAxis = 'x'|'y';

export {position,direction,collisionAxis}